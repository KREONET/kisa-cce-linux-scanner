# KISA platform semantics

## Scope

The scanner implements one result slot for each of the 67 Unix-server criteria in the rendered KISA CCE 2026 guide. The rendered pages explicitly distinguish Red Hat, Debian, Ubuntu, CentOS, or RHEL versions in nine criteria. The other 58 criteria state a generic Linux or product procedure. The table separates guide requirements from distribution adapters and scanner policy extensions, including any implemented threshold that is more specific than the rendered text.

This document records the guide-to-code boundary. `Guide` below means a requirement visible on the rendered criterion page. `Extension` means scanner logic needed to collect equivalent evidence on a supported distribution; it is not a claim made by the guide. Ubuntu derivatives route through their declared Ubuntu base and Enterprise Linux derivatives route through their declared RHEL-compatible base. The final column records the implemented result boundary for each criterion; this document does not assert one global fail-closed rule across every parser and evidence source.

## Criterion coverage matrix

| Criterion and title | Rendered guide branch | Debian and Ubuntu adapter | RHEL and Enterprise Linux adapter | Implemented automatic-verdict boundary |
|---|---|---|---|---|
| [U-01 root 계정 원격 접속 제한](https://kreonet.github.io/kisa-cce-guide-web/unix/u-01/) | **Explicit:** Linux Telnet and SSH procedures; CentOS 8 and Ubuntu 20.04+ Telnet note. | **Guide:** block direct root access. **Extension:** prefer live `sshd -T` output, parse the standard static drop-in form, mark other `Include` or `Match` graphs ambiguous, and inspect systemd/inetd/xinetd Telnet activation, effective xinetd service IDs, `/etc/pam.d/login`, and `/etc/securetty`. | **Guide:** same control. **Extension:** use the same observed SSH, Telnet, and PAM evidence; never infer absence solely from the RHEL 8 note. | OpenSSH policy is applied only to an identified OpenSSH listener, process, or active unit; another daemon on TCP/22 is `MANUAL`. Unresolved listener, unit, SSH graph, PAM graph, or terminal policy is `MANUAL` or `ERROR`; only resolved inactivity or an observed blocked root path is `GOOD`. |
| [U-02 비밀번호 관리정책 설정](https://kreonet.github.io/kisa-cce-guide-web/unix/u-02/) | **Explicit:** separate Redhat and Debian procedures. | **Guide:** Debian `common-password`, `pwquality.conf`, minimum length and exact credits, aging, and a `remember=N` history setting. **Extension:** resolve PAM includes, module arguments, and supported `pwhistory.conf`, and require a history value of at least four. | **Guide:** Redhat `system-auth`, quality/history files, root enforcement, and `/etc/security/opasswd`. **Extension:** resolve authselect and assess `system-auth` and `password-auth` independently. | Unsafe paths or incomplete PAM graphs are `ERROR`; bypassable controls are `VULNERABLE`; a Redhat `PASS_MIN_DAYS` value of `0` makes an otherwise passing result `MANUAL` because the page also states `1`, while a conforming value of `1` or greater does not trigger that conflict path. |
| [U-03 계정 잠금 임계값 설정](https://kreonet.github.io/kisa-cce-guide-web/unix/u-03/) | **Explicit:** Redhat tally, authselect, and faillock variants plus Debian PAM example. | **Guide:** a deny threshold of ten or fewer. **Extension:** expand Debian `common-auth` and `common-account`, including bracketed controls, and verify module availability and reachable reset flow. | **Guide:** tally or faillock, with RHEL 8+ authselect guidance. **Extension:** validate `system-auth` and `password-auth` independently and bind `faillock.conf` only to `pam_faillock`. | Missing modules, invalid authselect, unsafe include graphs, thresholds outside 1–10, or absent failure/reset flow cannot return `GOOD`; complex but plausible flows are `MANUAL`. |
| [U-04 비밀번호 파일 보호](https://kreonet.github.io/kisa-cce-guide-web/unix/u-04/) | **Generic Linux:** use shadow storage or a recognized encrypted password field in `/etc/passwd`. | **Guide:** accept shadow-backed or directly encrypted password storage. **Extension:** correlate `x` fields with `/etc/shadow` and validate recognized direct-field formats. | **Guide:** identical Linux path and threshold. **Extension:** identical safe-path and record checks. | Plain non-shadow fields are `VULNERABLE`; an unmatched shadow reference or unknown encrypted format is `MANUAL`; missing or unsafe required account evidence is `ERROR`; recognized shadow-backed, directly encrypted, or locked-only `!`/`*` fields can be `GOOD`. |
| [U-05 root 이외의 UID가 ‘0’ 금지](https://kreonet.github.io/kisa-cce-guide-web/unix/u-05/) | **Generic Unix/Linux judgment:** no other account may share root's UID. | **Guide:** compare every local account UID with root's UID. **Extension:** also require the root account itself to use UID 0. | **Guide:** same threshold. **Extension:** use the same local account adapter. | A nonzero root UID or another account sharing root's UID is `VULNERABLE`; unreadable or incomplete account data is `ERROR`; a complete unique root UID 0 result is `GOOD`. |
| [U-06 사용자 계정 su 기능 제한](https://kreonet.github.io/kisa-cce-guide-web/unix/u-06/) | **Generic Linux:** group permission or PAM restriction. | **Guide:** restrict `su` to an administrative group. **Extension:** expand `/etc/pam.d/su`, structurally identify the enforcing `pam_wheel` module field, bind that row to its own final `group=` option, verify module and group presence, and inspect `/usr/bin/su` metadata. Group membership suitability remains external policy. | **Guide:** same control. **Extension:** resolve authselect-aware PAM and the native `wheel` group with the same boundary. | Missing or non-executable `su` is `NOT_APPLICABLE`; no local general account is `MANUAL`; an unbypassable `wheel` or `sudo` PAM restriction with available module, existing group, and conforming `su` owner/mode can be `GOOD` without approving the group's members; module-name text inside another module's arguments is ignored; nonstandard groups, bracketed flows, unmanaged authselect, or permission-only restriction are `MANUAL`; no restriction or a missing restriction module/group is `VULNERABLE`. |
| [U-07 불필요한 계정 제거](https://kreonet.github.io/kisa-cce-guide-web/unix/u-07/) | **Generic Linux:** remove accounts not required for operations. | **Guide:** review `/etc/passwd` and login history. **Extension:** enumerate login-capable accounts using the effective `UID_MIN`. | **Guide:** same review. **Extension:** resolve the native `login.defs` precedence before enumeration. | Business need and recent legitimate use require organization evidence, so the result remains `MANUAL`; unreadable account or policy data is `ERROR`. |
| [U-08 관리자 그룹에 최소한의 계정 포함](https://kreonet.github.io/kisa-cce-guide-web/unix/u-08/) | **Generic Linux:** minimize root-group membership. | **Guide:** remove unnecessary root-group members. **Extension:** combine primary and supplementary membership from local account files. | **Guide:** same threshold. **Extension:** use the same account/group adapter. | No additional member is `GOOD`; any additional member is `MANUAL` pending an approved-role decision; unreadable or missing root-group data is `ERROR`. |
| [U-09 계정이 존재하지 않는 GID 금지](https://kreonet.github.io/kisa-cce-guide-web/unix/u-09/) | **Generic Unix/Linux judgment:** remove groups unnecessary for management or operations. | **Guide:** assess unnecessary groups. **Extension:** identify groups with no primary or valid supplementary account link. | **Guide:** same threshold. **Extension:** apply the same local account/group correlation. | Group necessity is organization-specific, so candidates and an empty candidate set remain `MANUAL`; malformed or unreadable databases are `ERROR`. |
| [U-10 동일한 UID 금지](https://kreonet.github.io/kisa-cce-guide-web/unix/u-10/) | **Generic Unix/Linux judgment:** unique user identifiers. | **Guide:** no accounts share a UID. **Extension:** compare all local `/etc/passwd` UID fields. | **Guide:** same threshold. **Extension:** identical local database check. | Any duplicate UID is `VULNERABLE`; unreadable data is `ERROR`; a complete unique set is `GOOD`. |
| [U-11 사용자 shell 점검](https://kreonet.github.io/kisa-cce-guide-web/unix/u-11/) | **Generic Linux:** non-login shells for accounts that do not require login. | **Guide:** standard service accounts use `/bin/false` or `/sbin/nologin`. **Extension:** recognize only `/bin/false`, `/usr/bin/false`, `/bin/nologin`, `/sbin/nologin`, and `/usr/sbin/nologin`, then use effective `UID_MIN` to surface other system accounts except `sync`, `shutdown`, and `halt`. | **Guide:** same list and shells. **Extension:** resolve native login defaults and apply the same literal shell-path allowlist. | A listed standard account outside the five-path allowlist is `VULNERABLE`; another system account outside it is `MANUAL`; shell executability or actual interactivity is not tested; unreadable policy/account data is `ERROR`. |
| [U-12 세션 종료 시간 설정](https://kreonet.github.io/kisa-cce-guide-web/unix/u-12/) | **Generic Linux:** `TMOUT` at most 600 seconds or csh `autologout` at most 10 minutes. | **Guide:** shell-global timeout. **Extension:** inspect `/etc/profile`, `/etc/bash.bashrc`, `/etc/profile.d`, and csh files. | **Guide:** same threshold. **Extension:** inspect `/etc/profile`, `/etc/bashrc`, `/etc/profile.d`, and csh files. | A missing required shell-global timeout is `VULNERABLE`; compliant, excessive, dynamic, unreadable, and conditional profile values remain `MANUAL` because later user startup files can change the final session value; invalid account or login-default evidence is `ERROR`. |
| [U-13 안전한 비밀번호 암호화 알고리즘 사용](https://kreonet.github.io/kisa-cce-guide-web/unix/u-13/) | **Explicit:** separate Redhat and Debian hash procedures. | **Guide:** SHA-256, SHA-512, or yescrypt. **Extension:** correlate valid `/etc/shadow` and direct `/etc/passwd` records with `pam_unix` and effective `ENCRYPT_METHOD`. | **Guide:** SHA-256 or SHA-512. **Extension:** evaluate both authselect PAM stacks and direct password records; classify valid platform-unlisted modern schemes separately from MD5 and DES. | Malformed records are `ERROR`; weak hashes, empty fields, weak PAM options, or a known weak future method are `VULNERABLE`; valid platform-unlisted schemes and unknown current formats are `MANUAL`; absent future-hash policy is `MANUAL` only when an observed unoptioned `pam_unix` path needs it, while a secure current hash can otherwise support `GOOD`; locked fields alone do not prove a secure algorithm. |
| [U-14 root 홈, 패스 디렉터리 권한 및 패스 설정](https://kreonet.github.io/kisa-cce-guide-web/unix/u-14/) | **Generic Linux:** current-directory elements must not precede trusted PATH entries. | **Guide:** remove leading or middle `.` entries. **Extension:** inspect root and global POSIX/C-shell files, including Debian bash paths and `profile.d`. | **Guide:** same rule. **Extension:** inspect the corresponding RHEL shell paths and `profile.d`. | Candidate empty or current-directory elements, inaccessible startup paths, and statically clean files remain `MANUAL` until the final root login environment is established; invalid account data or malformed/unsafe root-home syntax is `ERROR`. |
| [U-15 파일 및 디렉터리 소유자 설정](https://kreonet.github.io/kisa-cce-guide-web/unix/u-15/) | **Generic Unix/Linux judgment:** no ownerless files or directories. | **Guide:** find missing users or groups. **Extension:** scan local filesystem roots without crossing mount boundaries; offline roots use their own account databases. | **Guide:** same threshold. **Extension:** identical filesystem adapter. | Findings are `VULNERABLE` only when the simple NSS parser classifies both account maps as files-only; action tokens or additional fields are treated as possible external NSS and keep findings `MANUAL`; traversal or metadata failure is `ERROR`; an offline zero-match scan is `MANUAL` because hidden mount boundaries are not established. |
| [U-16 /etc/passwd 파일 소유자 및 권한 설정](https://kreonet.github.io/kisa-cce-guide-web/unix/u-16/) | **Generic Linux:** root owner and mode 0644 or stricter. | **Guide:** check `/etc/passwd`. **Extension:** safe rooted metadata lookup. | **Guide:** identical path and threshold. **Extension:** identical metadata lookup. | Wrong owner or excessive mode is `VULNERABLE`; absent or unreadable metadata is `ERROR`; a resolved conforming file is `GOOD`. |
| [U-17 시스템 시작 스크립트 권한 설정](https://kreonet.github.io/kisa-cce-guide-web/unix/u-17/) | **Generic Linux:** init and systemd scripts owned by root without ordinary-user write. | **Guide:** inspect startup scripts. **Extension:** cover init and rc paths plus systemd administrator, runtime-generator, local, and vendor unit directories. | **Guide:** same control. **Extension:** apply the same systemd scope with RHEL rc paths, including distributions where `/etc/init.d` resolves to `/etc/rc.d/init.d`. | Directory symlink entry points are resolved and deduplicated before traversal; dangling file aliases remain incomplete evidence. Any non-root owner or group/other write is `VULNERABLE`; incomplete traversal is `ERROR`; no discovered startup path is `NOT_APPLICABLE`; conforming vendor-only or local paths are `GOOD`. |
| [U-18 /etc/shadow 파일 소유자 및 권한 설정](https://kreonet.github.io/kisa-cce-guide-web/unix/u-18/) | **Generic Linux:** root owner and mode 0400 or stricter. | **Guide:** check `/etc/shadow`. **Extension:** safe rooted metadata lookup. | **Guide:** identical path and threshold. **Extension:** identical metadata lookup. | Missing file or excessive permissions are `VULNERABLE`; unsafe or unreadable metadata is `ERROR`; exact or stricter resolved metadata is `GOOD`. |
| [U-19 /etc/hosts 파일 소유자 및 권한 설정](https://kreonet.github.io/kisa-cce-guide-web/unix/u-19/) | **Generic Linux:** root owner and mode 0644 or stricter. | **Guide:** check `/etc/hosts`. **Extension:** safe rooted metadata lookup. | **Guide:** identical path and threshold. **Extension:** identical metadata lookup. | Wrong metadata is `VULNERABLE`; absent or unreadable evidence is `ERROR`; a complete conforming check is `GOOD`. |
| [U-20 /etc/(x)inetd.conf 파일 소유자 및 권한 설정](https://kreonet.github.io/kisa-cce-guide-web/unix/u-20/) | **Generic Linux:** inetd, xinetd, and systemd manager configuration owned by root and mode 0600 or stricter. | **Guide:** inspect inetd, xinetd, and systemd manager files. **Extension:** enumerate regular configuration files under `/etc/xinetd.d` and `/etc/systemd` without treating enabled-unit symlinks as their vendor targets. | **Guide:** same threshold and paths. **Extension:** apply the same confined enumeration. | A metadata violation is `VULNERABLE`; masks do not count as inspected files; unsafe paths or incomplete enumeration are `ERROR`; no applicable regular file is `NOT_APPLICABLE`. |
| [U-21 /etc/(r)syslog.conf 파일 소유자 및 권한 설정](https://kreonet.github.io/kisa-cce-guide-web/unix/u-21/) | **Generic Linux:** allowed owner root, bin, or sys and mode 0640 or stricter. | **Guide:** check the main syslog or rsyslog configuration path and metadata. **Extension:** resolve the standard `/etc/rsyslog.d/*.conf` graph within the scan root. | **Guide:** same main-path owner and mode threshold. **Extension:** apply the same confined split-file graph resolution. | Violations are `VULNERABLE`; unsafe metadata or traversal is `ERROR`; nested or nonstandard includes are `MANUAL`; masks do not establish a successful metadata check. |
| [U-22 /etc/services 파일 소유자 및 권한 설정](https://kreonet.github.io/kisa-cce-guide-web/unix/u-22/) | **Generic Unix/Linux judgment:** owner root, bin, or sys and mode 0644 or stricter. | **Guide:** inspect `/etc/services`. **Extension:** safe rooted metadata lookup. | **Guide:** identical path and threshold. **Extension:** identical lookup. | Wrong metadata is `VULNERABLE`; missing or unreadable evidence is `ERROR`; a resolved conforming file is `GOOD`. |
| [U-23 SUID, SGID, Sticky bit 설정 파일 점검](https://kreonet.github.io/kisa-cce-guide-web/unix/u-23/) | **Generic Linux:** remove unnecessary root-owned SUID and SGID permissions. | **Guide:** inventory root-owned privileged files. **Extension:** scan every resolved local filesystem root with `xdev`. | **Guide:** same threshold. **Extension:** identical filesystem scan. | With a complete live mount inventory, an empty finding set is technical `GOOD`; each finding is technical `MANUAL` pending package provenance and business approval; an incomplete mount inventory or traversal is `ERROR`. |
| [U-24 사용자, 시스템 환경변수 파일 소유자 및 권한 설정](https://kreonet.github.io/kisa-cce-guide-web/unix/u-24/) | **Generic Linux:** owner root or the account and no write by other users. | **Guide:** inspect listed home environment files. **Extension:** include bash, zsh, csh, ksh, X session, fish, PAM environment, and `environment.d` inputs. | **Guide:** same threshold. **Extension:** apply the same expanded home-file inventory. | Wrong ownership or group/other write is `VULNERABLE`; invalid account data, unsafe paths, or incomplete traversal are `ERROR`; all discovered files conforming is `GOOD`. |
| [U-25 world writable 파일 점검](https://kreonet.github.io/kisa-cce-guide-web/unix/u-25/) | **Generic Linux:** no world-writable file, or each file has a known approved reason. | **Guide:** inventory world-writable regular files. **Extension:** scan every resolved local filesystem root without crossing mounts. | **Guide:** same threshold. **Extension:** identical filesystem scan. | A complete zero-match live scan is `GOOD`; findings are `MANUAL` because approval is external; incomplete mount inventory or traversal is `ERROR`; offline zero-match is `MANUAL`. |
| [U-26 /dev에 존재하지 않는 device 파일 점검](https://kreonet.github.io/kisa-cce-guide-web/unix/u-26/) | **Generic Linux:** remove ordinary files masquerading as device files, excluding the guide's volatile `mqueue` and `shm` locations. | **Guide:** search `/dev` for regular files and exclude `/dev/mqueue` and `/dev/shm`. **Extension:** perform a no-follow, single-filesystem scan with the same explicit exclusions for offline roots. | **Guide:** identical path, exclusions, and rule. **Extension:** identical scan. | Any other regular file is `VULNERABLE`; missing `/dev`, an unsafe path, or incomplete traversal is `ERROR`; zero findings is `GOOD`. |
| [U-27 $HOME/.rhosts, hosts.equiv 사용 금지](https://kreonet.github.io/kisa-cce-guide-web/unix/u-27/) | **Generic Unix/Linux judgment:** disable r-services or restrict trust files to owner, mode 0600, and no `+`. | **Guide:** assess `/etc/hosts.equiv` and each `.rhosts`. **Extension:** inspect named systemd units and template definitions, SysV, inetd `exec/login/shell` aliases, xinetd, and TCP listeners; instantiated template units are not enumerated. | **Guide:** same threshold. **Extension:** apply the same service and trust-file checks. | Proven inactivity can be `GOOD`; an unidentified TCP listener, unreadable legacy activation source, or another incomplete activation graph is `MANUAL`; active service with an observed unsafe trust entry is `VULNERABLE`; invalid account data or unreadable trust evidence is `ERROR`. An inline-comment `+` token can be treated as an active trust entry and therefore requires report review. |
| [U-28 접속 IP 및 포트 제한](https://kreonet.github.io/kisa-cce-guide-web/unix/u-28/) | **Generic Linux:** TCP Wrapper, iptables, firewalld, or UFW restrictions. | **Guide:** restrict approved hosts and ports. **Extension:** collect candidate restrictions from live or persistent UFW, nftables, iptables, TCP Wrapper, and related input paths. | **Guide:** same requirement. **Extension:** collect firewalld zones and policies plus native nftables and iptables evidence. | A detected default-deny path with a source-specific port or service allowance is `MANUAL` pending the approved allowlist; no candidate is `VULNERABLE`. Backend handling is not uniform: UFW expects both raw address-family sections, a single nftables `table ip` can qualify, a confirmed provider can mask another provider's status-1 outcome, and firewalld policy-list failure can remain `MANUAL`; unsafe paths and other active-provider collection failures are `ERROR`. Runtime and persistent firewalld results are compared by classification, not exact rule identity. |
| [U-29 hosts.lpd 파일 소유자 및 권한 설정](https://kreonet.github.io/kisa-cce-guide-web/unix/u-29/) | **Generic Linux:** file absent, or root owned and mode 0600 or stricter. | **Guide:** inspect `/etc/hosts.lpd`. **Extension:** safe rooted metadata lookup. | **Guide:** identical path and threshold. **Extension:** identical lookup. | True absence or conforming metadata is `GOOD`; wrong metadata is `VULNERABLE`; dangling links, unsafe paths, or unreadable metadata are `ERROR`. |
| [U-30 UMASK 설정 관리](https://kreonet.github.io/kisa-cce-guide-web/unix/u-30/) | **Generic Linux:** effective system and user startup UMASK values must be 022 or more restrictive; active vsFTP and ProFTP paths use the same threshold. | **Guide:** inspect `/etc/profile`, `/etc/login.defs`, user startup files, vsFTP `local_umask`, and ProFTP `Umask`. **Extension:** resolve root-confined shell source graphs, Debian bash and zsh paths, csh/tcsh, ksh `ENV`, fish inputs, native PAM session stacks, `/etc/default/login`, `/etc/vsftpd.conf`, and `/etc/proftpd/proftpd.conf`. | **Guide:** same threshold. **Extension:** apply native RHEL bash, zsh, csh/tcsh, ksh `ENV`, fish, `postlogin`, and related PAM paths, plus `/etc/vsftpd/vsftpd.conf` and `/etc/proftpd.conf`. | A confirmed effective weak path is `VULNERABLE`; ordered safe declarations are `GOOD`; unresolved shell sources, mixed native PAM stack coverage, or an uncertain active FTP invocation are `MANUAL`; unsafe policy paths are `ERROR`. |
| [U-31 홈디렉토리 소유자 및 권한 설정](https://kreonet.github.io/kisa-cce-guide-web/unix/u-31/) | **Generic Linux:** each configured home belongs to its account and other users cannot write. | **Guide:** compare every `/etc/passwd` home that exists with its account and metadata. | **Guide:** apply the same account and home comparison. | Any owner mismatch or other-write bit is `VULNERABLE`; invalid account data or metadata/path failure is `ERROR`; all existing configured homes conforming is `GOOD`. |
| [U-32 홈 디렉토리로 지정한 디렉토리의 존재 관리](https://kreonet.github.io/kisa-cce-guide-web/unix/u-32/) | **Generic Linux:** every account's configured home exists. | **Guide:** compare every `/etc/passwd` home with the filesystem. | **Guide:** apply the same account and home comparison. | A missing configured home is `VULNERABLE`; invalid account data, unreadable evidence, or unsafe paths are `ERROR`; a complete match is `GOOD`. |
| [U-33 숨겨진 파일 및 디렉토리 검색 및 제거](https://kreonet.github.io/kisa-cce-guide-web/unix/u-33/) | **Generic Unix/Linux judgment:** remove unnecessary or suspicious hidden paths. | **Guide:** search and review hidden paths. **Extension:** scan each resolved local filesystem root without following links. | **Guide:** same review. **Extension:** identical scan. | Findings are `MANUAL` because necessity and integrity need review; incomplete mount inventory or traversal is `ERROR`; only a complete live zero-match scan is `GOOD`. |
| [U-34 Finger 서비스 비활성화](https://kreonet.github.io/kisa-cce-guide-web/unix/u-34/) | **Generic Linux:** disable Finger in inetd or xinetd. | **Guide:** Finger inactive. **Extension:** inspect systemd units and sockets, inetd/xinetd files, and port 79 listeners. | **Guide:** same threshold. **Extension:** inspect the corresponding RHEL unit and listener aliases. | Any activation path is `VULNERABLE`; unresolved or custom activation is `MANUAL`; fully resolved inactivity is `GOOD`. |
| [U-35 공유 서비스에 대한 익명 접근 제한 설정](https://kreonet.github.io/kisa-cce-guide-web/unix/u-35/) | **Generic Linux:** restrict anonymous FTP, NFS, and Samba access. | **Guide:** inspect FTP accounts and daemon settings, NFS exports, and Samba guest access. **Extension:** inspect standard vsFTP and ProFTP paths, defer ProFTP includes or custom invocation, inspect NFS anonymous mappings, and use native Samba evidence. | **Guide:** same services. **Extension:** apply the corresponding RHEL standard paths and provider aliases. | Explicit anonymous or guest enablement is `VULNERABLE`; ProFTP includes, custom configuration, active NFS, or organization-dependent mappings are `MANUAL`; no active share provider is `NOT_APPLICABLE`; fully resolved FTP or Samba restrictions can be `GOOD`. |
| [U-36 r 계열 서비스 비활성화](https://kreonet.github.io/kisa-cce-guide-web/unix/u-36/) | **Generic Linux:** disable rlogin, rsh, and rexec across inetd, xinetd, and systemd. | **Guide:** all r-services inactive. **Extension:** inspect named units, sockets and template definitions, legacy files, listeners, trust-file use, and rsync daemon state; instantiated template units are not enumerated. | **Guide:** same threshold. **Extension:** apply the matching RHEL unit and listener aliases. | Active rlogin, rsh, or rexec without observed use evidence is `VULNERABLE`; observed use, active rsync, or unresolved activation is `MANUAL`; complete inactivity is `GOOD`. |
| [U-37 crontab 설정파일 권한 설정 미흡](https://kreonet.github.io/kisa-cce-guide-web/unix/u-37/) | **Generic Linux:** restrict cron/at commands and keep related files root owned at the stated modes. | **Guide:** inspect command, spool, and policy files. **Extension:** map Debian `/var/spool/cron/crontabs` and `/var/spool/cron/atjobs`, and assess confined symlink targets. | **Guide:** same threshold. **Extension:** map RHEL `/var/spool/cron` and `/var/spool/at` with the same confined traversal. | Any discovered owner/mode violation, including a resolved symlink target, is `VULNERABLE`; unsafe paths or incomplete metadata are `ERROR`; absent optional scheduler components are excluded from applicability. |
| [U-38 DoS 공격에 취약한 서비스 비활성화](https://kreonet.github.io/kisa-cce-guide-web/unix/u-38/) | **Generic Linux:** disable echo, discard, daytime, and chargen, and review the need for NTP, DNS, SNMP, and SMTP. | **Guide:** inspect inetd, xinetd, systemd, and the listed auxiliary network services. **Extension:** resolve Debian units, sockets, legacy entries, and listeners. | **Guide:** same list. **Extension:** resolve RHEL unit and listener variants. | Active echo, discard, daytime, or chargen is `VULNERABLE`; active or unknown NTP, DNS, SNMP, or SMTP is `MANUAL`; unresolved legacy activation is `MANUAL`; complete inactivity is `GOOD`. |
| [U-39 불필요한 NFS 서비스 비활성화](https://kreonet.github.io/kisa-cce-guide-web/unix/u-39/) | **Generic Linux:** disable NFS when not required. | **Guide:** inspect NFS service state. **Extension:** detect NFS server unit aliases, `nfs-mountd`, and port 2049; exports are evaluated separately by U-40. | **Guide:** same decision. **Extension:** apply the corresponding native unit and listener aliases. | Active NFS is `MANUAL` because business need is external; resolved inactivity is `GOOD`; unresolved activation is `MANUAL`. |
| [U-40 NFS 접근 통제](https://kreonet.github.io/kisa-cce-guide-web/unix/u-40/) | **Generic Linux:** restrict exports and keep `/etc/exports` root owned at mode 0644 or stricter. | **Guide:** validate exports. **Extension:** inspect `/etc/exports` and confined `/etc/exports.d/*.exports` regular files or symlinks plus active Debian NFS state. | **Guide:** same threshold. **Extension:** inspect the same export graph and RHEL NFS state. | An active NFS service with observed wildcard access or unsafe metadata is `VULNERABLE`; approved-client scope, inactive residual exports, empty active exports, or complex options are `MANUAL`; an unsafe, unreadable, dangling, or escaping static export path, or failure to read the active `exportfs` table, is `ERROR`. |
| [U-41 불필요한 automountd 제거](https://kreonet.github.io/kisa-cce-guide-web/unix/u-41/) | **Generic Linux:** disable automount or autofs. | **Guide:** service inactive. **Extension:** resolve systemd aliases, SysV runlevel activation, named processes, and runtime state. | **Guide:** same threshold. **Extension:** apply the same native systemd, SysV, and process adapters. | Any activation is `VULNERABLE`; unknown activation is `MANUAL`; resolved inactivity is `GOOD`. |
| [U-42 불필요한 RPC 서비스 비활성화](https://kreonet.github.io/kisa-cce-guide-web/unix/u-42/) | **Generic Linux:** disable unnecessary RPC services. | **Guide:** inspect legacy and systemd RPC activation. **Extension:** enumerate dangerous RPC daemons, dotted and hyphenated unit aliases, SysV names, rpcbind, sockets, and listeners; NFS and other dependencies remain a manual review. | **Guide:** same control. **Extension:** map RHEL RPC aliases with the same dependency boundary. | Dangerous active RPC is `VULNERABLE`; NIS+ or general rpcbind/listener activity and unresolved activation are `MANUAL`; the scanner does not prove an NFS dependency; complete inactivity is `GOOD`. |
| [U-43 NIS, NIS+ 점검](https://kreonet.github.io/kisa-cce-guide-web/unix/u-43/) | **Explicit:** Linux daemon list and RHEL 8 package-removal note. | **Guide:** NIS inactive or NIS+ used. **Extension:** detect `ypserv`, `ypbind`, `ypxfrd`, `rpc.yppasswdd`, and `rpc.ypupdated` through named units and live RPC/listener evidence; inetd/xinetd activation is not inspected by this check. | **Guide:** same list; RHEL 8+ note. **Extension:** require observed state instead of inferring absence from the release. | Active legacy NIS is `VULNERABLE`; unresolved activation is `MANUAL`; proven inactive legacy NIS, or resolved NIS+-only use, is `GOOD`. |
| [U-44 tftp, talk 서비스 비활성화](https://kreonet.github.io/kisa-cce-guide-web/unix/u-44/) | **Explicit:** Linux procedures and RHEL 7+ talk-package removal note. | **Guide:** tftp, talk, and ntalk inactive. **Extension:** inspect inetd/xinetd, named units and template definitions, and listeners; instantiated template units are not enumerated. | **Guide:** same services; package-removal note. **Extension:** determine state from the host and include native unit aliases. | Any observed activation is `VULNERABLE`; unresolved activation is `MANUAL`; complete inactivity is `GOOD`. |
| [U-45 메일 서비스 버전 점검](https://kreonet.github.io/kisa-cce-guide-web/unix/u-45/) | **Generic Linux:** active Sendmail, Postfix, or Exim must be current. | **Guide:** compare an active mail version with the latest secure release. **Extension:** identify active Debian mail providers from units and listeners without collecting a local version. | **Guide:** same requirement. **Extension:** identify active RHEL mail providers from units and listeners without collecting a local version. | An active provider is always `MANUAL` until its local version and current vendor security status are reviewed; no active provider is `NOT_APPLICABLE`; unknown activation is `MANUAL`. |
| [U-46 일반 사용자의 메일 서비스 실행 방지](https://kreonet.github.io/kisa-cce-guide-web/unix/u-46/) | **Generic mail-service judgment:** prevent ordinary users from executing mail administration. | **Guide:** enforce execution restriction. **Extension:** evaluate Sendmail `restrictqrun` and root ownership plus other-execute permission on selected Postfix/Exim administration commands. Group ownership and membership are not evaluated. | **Guide:** same threshold. **Extension:** evaluate the corresponding installed provider and command paths. | Non-root ownership, other-execute permission, or observed Sendmail options without `restrictqrun` is `VULNERABLE`; absent Sendmail options and unknown provider/configuration are `MANUAL`; root-owned commands with no other-execute bit can be `GOOD` even if group execution remains; inactive SMTP is `NOT_APPLICABLE`. |
| [U-47 스팸 메일 릴레이 제한](https://kreonet.github.io/kisa-cce-guide-web/unix/u-47/) | **Generic mail-service judgment:** restrict unauthenticated relay. | **Guide:** mail relay must be controlled. **Extension:** resolve Postfix effective relay restrictions and networks; detect Sendmail `promiscuous_relay`; Exim is identified but its ACL evidence is not collected. | **Guide:** same threshold. **Extension:** use the same provider-specific boundary. | Open Postfix networks, permissive relay, or Sendmail `promiscuous_relay` is `VULNERABLE`; Sendmail maps, every Exim provider, custom invocations, and static-only Postfix remain `MANUAL`; runtime-validated restrictive Postfix can be `GOOD`; inactive SMTP is `NOT_APPLICABLE`. |
| [U-48 expn, vrfy 명령어 제한](https://kreonet.github.io/kisa-cce-guide-web/unix/u-48/) | **Generic mail-service judgment:** disable or restrict EXPN and VRFY. | **Guide:** Sendmail `noexpn` and `novrfy`. **Extension:** also evaluate Postfix `disable_vrfy_command` and collect limited Exim ACL values. | **Guide:** same control. **Extension:** apply the same installed-provider adapters. | Explicit missing Sendmail/Postfix controls or an accepting Exim ACL is `VULNERABLE`; absent Sendmail options, restrictive or statically collected Exim ACLs, custom invocation, and unknown providers are `MANUAL`; resolved restrictive Sendmail/Postfix paths and a runtime Exim result with empty ACL values can be `GOOD`; inactive SMTP is `NOT_APPLICABLE`. |
| [U-49 DNS 보안 버전 패치](https://kreonet.github.io/kisa-cce-guide-web/unix/u-49/) | **Generic Linux/BIND:** maintain a current security-patched version. | **Guide:** compare BIND with current ISC security releases. **Extension:** classify DNS/BIND activation from named units and listeners without recording provider identity or a local BIND version. | **Guide:** same requirement. **Extension:** include the RHEL versioned unit aliases with the same activation-only result. | Active named-unit DNS is `MANUAL` until its local version, current vendor advisories, and backported package status are evaluated; listener-only or otherwise unknown DNS is `MANUAL`; inactive DNS is `NOT_APPLICABLE`. |
| [U-50 DNS ZoneTransfer 설정](https://kreonet.github.io/kisa-cce-guide-web/unix/u-50/) | **Generic DNS/BIND judgment:** restrict zone transfer to authorized parties. | **Guide:** evaluate `allow-transfer`. **Extension:** use a trusted native result only for a standard invocation, flatten the selected configuration, and detect simple zone/global clauses and the BIND version default; custom invocation, views, ACLs, and per-zone inheritance are deferred. | **Guide:** same threshold. **Extension:** apply the corresponding RHEL main path and BIND variants. | An observed unrestricted authoritative zone is `VULNERABLE`; inactive or non-authoritative DNS is `NOT_APPLICABLE`; `GOOD` is limited to a validated simple global restriction or a recognized default-deny BIND version; includes, views, ACLs, per-zone restrictions, inheritance, or custom invocation remain `MANUAL`. |
| [U-51 DNS 서비스의 취약한 동적 업데이트 설정 금지](https://kreonet.github.io/kisa-cce-guide-web/unix/u-51/) | **Generic DNS/BIND judgment:** disable dynamic update or apply proper access control. | **Guide:** inspect update policy. **Extension:** flatten a validated standard BIND configuration and recognize explicit denial or simple key-only `allow-update` forms; includes, views, ACLs, `update-policy`, and external backends are deferred. | **Guide:** same threshold. **Extension:** apply the corresponding RHEL paths and BIND variants. | An unrestricted `allow-update` or `allow-update-forwarding` target is `VULNERABLE`; an `ANY` token inside `update-policy` is not treated as an unrestricted source and remains `MANUAL`; validated denial or simple key-only restriction can be `GOOD`; other restricted policies and unresolved graphs are `MANUAL`; inactive DNS is `NOT_APPLICABLE`. |
| [U-52 Telnet 서비스 비활성화](https://kreonet.github.io/kisa-cce-guide-web/unix/u-52/) | **Generic Linux:** disable Telnet across inetd, xinetd, or systemd and use SSH. | **Guide:** Telnet inactive. **Extension:** resolve Debian units, sockets, legacy files, and port 23 listeners. | **Guide:** same threshold. **Extension:** resolve matching RHEL unit aliases and listener evidence. | Any activation is `VULNERABLE`; unknown/custom activation is `MANUAL`; fully resolved inactivity is `GOOD`. |
| [U-53 FTP 서비스 정보 노출 제한](https://kreonet.github.io/kisa-cce-guide-web/unix/u-53/) | **Generic Linux:** suppress product/version information in vsFTP or ProFTP banners. | **Guide:** safe `ftpd_banner` or `ServerIdent`. **Extension:** inspect standard Debian config paths and provider aliases; custom unit arguments, ProFTP `Include`, and `VirtualHost` are detected and deferred. | **Guide:** same settings. **Extension:** apply the corresponding RHEL standard paths and provider aliases. | Default or explicit product, distribution, hostname, or version disclosure is `VULNERABLE`; an unrelated policy number is not a version disclosure; custom invocation, includes, dynamic banners, or unrecognized wording are `MANUAL`; `GOOD` requires `ServerIdent off` or a resolved non-identifying banner containing recognized warning language; inactive FTP is `NOT_APPLICABLE`. |
| [U-54 암호화되지 않는 FTP 서비스 비활성화](https://kreonet.github.io/kisa-cce-guide-web/unix/u-54/) | **Generic Linux:** disable unencrypted FTP. | **Guide:** remove legacy/plain FTP activation. **Extension:** treat an active vsFTP or ProFTP service as encrypted only when control and data TLS are both forced. | **Guide:** same requirement. **Extension:** apply the same TLS-forcing test to native config paths and units. | Plain or optional TLS is `VULNERABLE`; custom or unresolved TLS configuration is `MANUAL`; inactive FTP or fully forced TLS is `GOOD`. |
| [U-55 FTP 계정 shell 제한](https://kreonet.github.io/kisa-cce-guide-web/unix/u-55/) | **Generic Linux:** FTP accounts use `/bin/false` or `/sbin/nologin`. | **Guide:** inspect the FTP account shell. **Extension:** inspect only the local account named `ftp`. | **Guide:** same threshold. **Extension:** apply the same literal-account check. | An `ftp` account with an interactive or unknown shell is `VULNERABLE`; a recognized non-login shell is `GOOD`; no literal `ftp` account is `NOT_APPLICABLE`; unreadable passwd data is `ERROR`. |
| [U-56 FTP 서비스 접근 제어 설정](https://kreonet.github.io/kisa-cce-guide-web/unix/u-56/) | **Generic FTP judgment:** restrict access to approved IP addresses or hosts. | **Guide:** access control must match allowed clients. **Extension:** identify active FTP providers and report that daemon, systemd, host-firewall, and external-firewall policy require review. | **Guide:** same requirement. **Extension:** apply the same active-provider boundary. | An organization-approved client allowlist is not embedded and no access-control rules are collected, so active or uncertain FTP remains `MANUAL`; inactive FTP is `NOT_APPLICABLE`. |
| [U-57 Ftpusers 파일 설정](https://kreonet.github.io/kisa-cce-guide-web/unix/u-57/) | **Generic FTP judgment:** deny root login. | **Guide:** inspect ftpusers files, vsFTP user-list controls, and ProFTP `UseFtpUsers` or `RootLogin`. **Extension:** identify the active provider and select the standard Debian control paths. | **Guide:** same controls and threshold. **Extension:** select the corresponding RHEL standard paths. | A proven root-allow path is `VULNERABLE`; custom unit paths, ProFTP includes, unsupported providers, or conflicting controls are `MANUAL`; resolved root denial is `GOOD`; inactive FTP is `NOT_APPLICABLE`. PAM graphs are not expanded by this check. |
| [U-58 불필요한 SNMP 서비스 구동 점검](https://kreonet.github.io/kisa-cce-guide-web/unix/u-58/) | **Generic Linux:** SNMP should be inactive when unnecessary. | **Guide:** assess `snmpd`. **Extension:** detect units, sockets, custom invocation, and UDP listeners. | **Guide:** same threshold. **Extension:** map RHEL unit aliases and the same listener evidence. | Any observed activation is `VULNERABLE`; unknown state is `MANUAL`; fully resolved inactivity is `GOOD`. |
| [U-59 안전한 SNMP 버전 사용](https://kreonet.github.io/kisa-cce-guide-web/unix/u-59/) | **Generic SNMP judgment:** version 3 or later. | **Guide:** v1/v2 is vulnerable. **Extension:** parse standard Debian Net-SNMP files and v1/v2c community/VACM evidence, while detecting unresolved includes and custom invocation. | **Guide:** same threshold. **Extension:** apply the RHEL standard paths with the same include boundary. | Proven v1/v2c is `VULNERABLE`; apparent v3 or any unresolved include remains `MANUAL` until authenticated/encrypted runtime behavior is established; inactive SNMP is `NOT_APPLICABLE`. |
| [U-60 SNMP Community String 복잡성 설정](https://kreonet.github.io/kisa-cce-guide-web/unix/u-60/) | **Explicit:** separate Redhat `com2sec` and Debian `rocommunity`/`rwcommunity` syntax. | **Guide:** Debian community syntax and stated length/character combinations. **Extension:** parse standard v2c directives without emitting secrets. | **Guide:** Redhat `com2sec` syntax. **Extension:** parse source/name shifts such as `-Cn` on standard paths. | Any weak v2c string is `VULNERABLE`, including mixed v2c/v3 configuration; fully resolved strong v2c-only strings may be `GOOD`; v3 secrets, custom paths, dialect mismatches, or unresolved includes are `MANUAL`; inactive SNMP is `NOT_APPLICABLE`. |
| [U-61 SNMP Access Control 설정](https://kreonet.github.io/kisa-cce-guide-web/unix/u-61/) | **Explicit:** separate Redhat `com2sec` and Debian community source fields. | **Guide:** `rocommunity` and `rwcommunity` must name an allowed network. **Extension:** normalize all-network aliases and masks. | **Guide:** `com2sec` must name an allowed network. **Extension:** parse normal and `-Cn` field layouts. | Default/all-network sources are `VULNERABLE`; `GOOD` requires exactly one fully resolved v2c community with a specific source; multiple restricted communities, v3, custom paths, dialect mismatches, or unresolved access graphs are `MANUAL`; inactive SNMP is `NOT_APPLICABLE`. |
| [U-62 로그인 시 경고 메시지 설정](https://kreonet.github.io/kisa-cce-guide-web/unix/u-62/) | **Generic Linux and service procedures:** server, Telnet, SSH, FTP, SMTP, and DNS warnings. | **Guide:** configure login and active-service banners. **Extension:** inspect standard Debian OpenSSH, FTP, Exim/Postfix/Sendmail, and BIND warning surfaces and active providers. | **Guide:** same service set. **Extension:** inspect the RHEL standard paths and provider aliases. | A missing applicable warning is `VULNERABLE`; nonempty but unrecognized wording and dynamic/custom paths are `MANUAL`; all applicable surfaces with recognized warning language can be `GOOD`. Product or version identity is not separately detected, so its presence does not prevent `GOOD`. |
| [U-63 sudo 명령어 접근 관리](https://kreonet.github.io/kisa-cce-guide-web/unix/u-63/) | **Generic Linux:** `/etc/sudoers` owned by root with mode exactly 0640 in the good example. | **Guide:** inspect `/etc/sudoers`. **Extension:** select sudo or Ubuntu sudo-rs and validate only the active main policy path; the sudoers include graph is not assessed by this check. | **Guide:** identical guide path. **Extension:** resolve the installed provider and main-file metadata. | No provider is `NOT_APPLICABLE`; an active provider with no policy is `VULNERABLE`; wrong owner or mode above 0640 is `VULNERABLE`; stricter-than-0640 mode and sudo-rs-only policy are `MANUAL`; unreadable/unsafe policy is `ERROR`; exact root:0640 main-file metadata is `GOOD` without include-graph validation. |
| [U-64 주기적 보안 패치 및 벤더 권고사항 적용](https://kreonet.github.io/kisa-cce-guide-web/unix/u-64/) | **Generic Linux:** supported OS/kernel and an established, recurring vendor-advisory patch process. | **Guide:** verify policy and current patch application. **Extension:** record the Debian/Ubuntu package system, selected local package-history mtimes, package-manager command presence, platform base, and running kernel when available. | **Guide:** same requirement. **Extension:** record the corresponding RPM/DNF/YUM metadata and running-kernel evidence. | The scanner does not evaluate lifecycle windows, reboot requirements, or current advisories and never returns `GOOD` from local metadata alone; this criterion remains `MANUAL`. |
| [U-65 NTP 및 시각 동기화 설정](https://kreonet.github.io/kisa-cce-guide-web/unix/u-65/) | **Explicit:** generic NTP plus RHEL 8+ Chrony note and procedure. | **Guide:** configured and synchronized time service. **Extension:** select observed NTPsec, Chrony, or systemd-timesyncd; require configured sources, runtime synchronization, and persistent activation. | **Guide:** RHEL 8+ Chrony with `/etc/chrony.conf`. **Extension:** route that branch to Chrony; other active providers remain implementation-only evidence. | No selected synchronized source is `VULNERABLE`; missing configuration or persistence is `MANUAL`; collection failure is `ERROR`; even synchronized persistent service is `MANUAL` without an organization-approved source allowlist, so no `GOOD` path exists. |
| [U-66 정책에 따른 시스템 로깅 설정](https://kreonet.github.io/kisa-cce-guide-web/unix/u-66/) | **Generic Linux:** required facilities and severities must be logged according to policy. | **Guide:** rsyslog facility routing. **Extension:** report Debian/Ubuntu expected system, authentication, mail, and scheduler file destinations, and record journald-only evidence. | **Guide:** same facility set. **Extension:** report the corresponding RHEL file destinations. | No active live logger is `VULNERABLE`; a present logger, including one with missing expected routes, remains `MANUAL` pending organization-policy review; unsafe collection is `ERROR`. Console and emerg routes are not reported separately. |
| [U-67 로그 디렉터리 소유자 및 권한 설정](https://kreonet.github.io/kisa-cce-guide-web/unix/u-67/) | **Generic Linux:** `/var/log` regular files owned by root and mode 0644 or stricter. | **Guide:** inspect log-file metadata. **Extension:** recursively enumerate regular files, directories, symlinks, and mount boundaries; safe in-root file symlink targets are included in the file assessment. | **Guide:** identical threshold. **Extension:** identical traversal. | A regular-file or safe file-symlink-target violation is `VULNERABLE`; missing paths and metadata/traversal failures are `ERROR`; empty or offline scope, directory violations, directory/dangling/out-of-root symlinks, or excluded mounts are `MANUAL`; a complete live conforming file set and conforming directories can be `GOOD`. |

## Versioned native behavior

The product identity and upstream configuration base are separate. Approved Ubuntu derivatives use the corresponding Ubuntu base behavior, and approved Enterprise Linux derivatives use the corresponding RHEL major behavior.

| Native behavior | Version routing |
|---|---|
| PAM password history configuration | Debian 13, Ubuntu 24.04 and 26.04, their approved derivatives, and supported Enterprise Linux targets may use `pwhistory.conf`. Debian 12 and Ubuntu 22.04-family targets require effective PAM module arguments when the older implementation lacks that file. |
| Login-default precedence | General login-default consumers use their native legacy or libeconf behavior. For `pam_umask`, Debian and Enterprise Linux 8 read `/etc/login.defs`; Enterprise Linux 9 applies `/usr/share` before `/etc` while a higher-priority same-basename file masks the lower file; Enterprise Linux 10 uses `/etc/login.defs` and `/etc/login.defs.d/*.defs` only. |
| Authselect | Supported RHEL-family targets validate managed, unmanaged, and invalid authselect states. `system-auth` and `password-auth` are assessed separately. |
| systemd sysctl credentials | A stock `LoadCredential=sysctl.extra` declaration is not itself an override. Only supplied credential material makes the loader graph indeterminate. Debian/Ubuntu UFW sysctl files are considered only when UFW is active. |
| Time synchronization | Enterprise Linux 8 and later route the KISA procedure to Chrony. Debian-family providers are selected from observed service state and validated with provider-specific evidence. |
| Service variants | Version-specific units such as RHEL 8 `named-pkcs11` and `named-sdb`, socket activation, and Ubuntu 26.04 sudo-rs provider selection are included in applicability resolution. |

## Validation boundary

The generated-fixture suite verifies every accepted `/etc/os-release` identity, all 67 result slots, the explicit KISA family branches above, and high-risk native parser and service variants. These tests establish deterministic scanner behavior; booted-host acceptance has not been performed.

Before a release can claim booted-host product acceptance, run the full scanner on every row in the dated [platform support matrix](platform-support.md), review every `GOOD` result against the target's effective native state, and retain the reports and package versions. The current claim is rendered-guide branch coverage plus deterministic fixture coverage; booted-host acceptance has not been performed.
